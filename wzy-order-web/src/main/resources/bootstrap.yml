spring:
  cloud:
    config:
      # uri: http://localhost:12000
      discovery:
        enabled: true
        service-id: wzy-config-server

      name: order
      profile: dev
      label: trunk

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8888/eureka/